package com.undef.superahorro.ui.theme

import androidx.compose.ui.graphics.Color

// =====================================================
// PALETA "ÍNDIGO COSMOS" — SuperAhorro
// Colores primarios: Índigo profundo + Coral vibrante
// Transmite: modernidad, confianza, energía
// =====================================================

// --- Primarios ---
/**
 * Todos los colores de la app con la paleta "Índigo Cosmos".
 * Acá están los colores principales, los de modo oscuro y los colores
 * de cada supermercado para las franjas de las tarjetas.
 */
val IndigoDeep    = Color(0xFF3D2C8D)  // Índigo profundo — color principal
val IndigoMedium  = Color(0xFF5C42A6)  // Índigo medio — variante activa
val IndigoLight   = Color(0xFF7B5FC0)  // Índigo claro — bordes y tints

// --- Acento / CTA ---
val CoralVibrant  = Color(0xFFFF6B6B)  // Coral vibrante — FAB, badges, confirmaciones
val CoralLight    = Color(0xFFFFE5E5)  // Coral claro — fondos de error suave

// --- Superficies claro ---
val SurfaceLight  = Color(0xFFF7F5FF)  // Blanco violáceo — fondo principal
val SurfaceWhite  = Color(0xFFFFFFFF)  // Blanco puro — cards

// --- Texto claro ---
val TextPrimary   = Color(0xFF1A1040)  // Casi negro violáceo — texto principal
val TextSecondary = Color(0xFF6B5FA0)  // Índigo grisáceo — texto secundario
val TextHint      = Color(0xFFB0A8D4)  // Lila suave — placeholders

// --- Estados ---
val ColorError        = Color(0xFFD32F2F)
val ColorErrorLight   = Color(0xFFFFEBEE)
val ColorWarning      = Color(0xFFF57F17)
val ColorWarningLight = Color(0xFFFFF3E0)
val ColorSuccess      = Color(0xFF2E7D52)
val ColorSuccessLight = Color(0xFFE8F5E9)

// --- Divisores y bordes ---
val DividerColor  = Color(0xFFE8E4F8)
val BorderColor   = Color(0xFFD4CCF0)

// --- Superficies oscuro ---
val DarkBackground  = Color(0xFF0D0920)  // Casi negro índigo
val DarkSurface     = Color(0xFF1A1535)  // Índigo muy oscuro
val DarkCard        = Color(0xFF241E48)  // Card oscura
val DarkTextPrimary = Color(0xFFEDE9FF)  // Blanco violáceo — texto claro
val DarkTextSecond  = Color(0xFFB0A8D4)  // Lila suave — texto secundario oscuro

// --- Colores por supermercado (franja lateral en cards) ---
val ColorCoto      = Color(0xFFE53935)  // Rojo Coto
val ColorCarrefour = Color(0xFF1E88E5)  // Azul Carrefour
val ColorDia       = Color(0xFFFB8C00)  // Naranja Día
val ColorJumbo     = Color(0xFF8E24AA)  // Púrpura Jumbo
val ColorWalmart   = Color(0xFF00897B)  // Verde Walmart
val ColorDefault   = Color(0xFF546E7A)  // Gris azulado — otros
